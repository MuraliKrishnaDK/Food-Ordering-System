[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/xwiggy-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/menu"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
